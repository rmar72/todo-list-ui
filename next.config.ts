import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['s3-alpha-sig.figma.com', 'cdn-icons-png.flaticon.com', 'icons.veryicon.com', 'static-00.iconduck.com'],
  },

};

export default nextConfig;
